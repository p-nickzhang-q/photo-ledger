# Photo Ledger

一个纯本地运行的 Android 记账 App：用户上传订单截图，端侧多模态模型将其提取为结构化账目记录。无账号、无云端、数据不出设备。运行时为 llama.cpp（自建 JNI 绑定），模型主选 Qwen3-VL-4B。

## Language

**订单截图 (Order Screenshot)**:
主要输入源：电商/支付 App 的订单详情页截图（如淘宝、京东、美团、微信/支付宝），而非纸质小票或发票。
_Avoid_: 票据（泛称，容易误以为以纸质小票为主）

**账目记录 (Entry)**:
一条记账条目，由订单截图提取生成或用户手工录入，含金额、日期、商家、类别等字段。
_Avoid_: 流水、交易、订单（"订单"保留给截图里的原订单，不用于账目条目）

**提取 (Extraction)**:
端侧模型将一张订单截图转换为结构化字段的过程，结果必须经用户确认/修正后才成为账目记录。
_Avoid_: 识别、OCR（实现细节，不进领域语言）

**提取结果 (Draft)**:
提取的原始输出，处于待确认状态；用户确认后才转为账目记录。Draft ≠ Entry。

**类别 (Category)**:
账目记录的归类标签，内置默认一套，允许用户自定义增删改。
_Avoid_: 分组、标签

**汇总级提取 (Summary-level Extraction)**:
一张截图 → 一条账目记录（商家、日期、总额、币种、类别），不要求逐商品明细行。

**币种 (Currency)**:
账目记录的货币单位，默认人民币，但作为显式字段保留以支持未来外币票据。

**硬字段 (Hard Field)**:
提取错误会直接造成账目错误的字段（总额、日期），是提取质量闸门的严要求对象。
_Avoid_: 关键字段、必填字段（混入了表单校验的含义）

**软字段 (Soft Field)**:
提取错误可被人一眼看出并低成本修正的字段（商家、类别建议），质量闸门要求宽于硬字段。

**订单状态 (Order Status)**:
截图中原订单的履约状态文本（如"待付款""已退款"）。v1 仅作为提取字段保留展示，不参与账目逻辑；"退款"不是 v1 的领域概念。

**质量闸门 (Quality Gate)**:
投入 Android 集成前，在桌面环境用真实截图对提取准确率的验收标准；未达标即触发备选路线讨论。
_Avoid_: 测试通过率（混入了单元测试的含义）

**实付款 (Actual Paid)**:
账目记录取用的金额口径：用户在该订单实际支付的钱。商品总价、运费、优惠是截图上的附属字段，不参与统计。
_Avoid_: 总额、订单金额（口径不明）

**付款时间 (Payment Time)**:
账目记录日期的首选口径：支出发生在付款那一刻；截图上缺失时退用下单时间。
_Avoid_: 日期（泛指，未说明取哪个时间）

**导入队列 (Import Queue)**:
一次批量进入的待提取截图集合，逐张处理，单张失败不阻塞后续。
_Avoid_: 批处理任务

**备份 (Backup)**:
用户手动导出的 JSON 文件，含账目记录与自定义类别，可再次导入恢复；照片不在备份内。
_Avoid_: 同步（暗示自动上云）

**导出 (Export)**:
面向人阅读的 CSV 文件（UTF-8 BOM），只出不进，不是恢复手段。与备份的区别：备份可回灌，导出不可。
_Avoid_: 备份（两者不可混用）
