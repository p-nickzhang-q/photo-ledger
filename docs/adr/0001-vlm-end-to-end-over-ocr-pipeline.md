# 提取路线选择 VLM 端到端，而非 OCR + 小模型两段式

订单截图是数字渲染的清晰文本图像（非拍照小票），处于端侧 VLM 的能力舒适区；端到端 VLM 管道单一、维护面小。备选路线 PaddleOCR + 小文本模型（OCR 约 20MB + 0.5-2B 文本模型）只作为 VLM 提取准确率验证失败时的备胎，v1 不实现。若触发备胎，需要重开讨论。

配套的验证闸门（在投入 Android 集成之前，先在桌面 llama.cpp 上用真实截图跑分）：硬字段（总额、日期）≥95% 完全正确，软字段（商家、类别）≥80% 且错误可一键修正。达不到即触发备胎路线讨论，不硬上。
